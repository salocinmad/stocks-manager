# ⚠️ NO EDITAR ESTE ARCHIVO MANUALMENTE ⚠️
# 
# Las credenciales se generan automáticamente al ejecutar docker compose up por primera vez.
# Este archivo es solo una plantilla de ejemplo.
#
# Para configurar variables opcionales (como FINNHUB_API_KEY), puedes crear un archivo .env
# en la raíz del proyecto con tus valores personalizados ANTES de ejecutar docker compose up.
#
# Ejemplo de .env personalizado (opcional):
#
# FINNHUB_API_KEY=tu_api_key_aqui
# PORT=5000
#
# Las credenciales de MariaDB (usuario, contraseña, JWT_SECRET, MASTER_PASSWORD)
# se generarán automáticamente de forma segura.

# API Keys (opcional - configurable desde el panel admin después de la instalación)
FINNHUB_API_KEY=

# Puerto del servidor (opcional - por defecto 5000)
PORT=5000

# Las siguientes variables se generan automáticamente en el primer arranque:
# - MYSQL_USER (usuario de base de datos)
# - MYSQL_PASSWORD (contraseña de base de datos)
# - MYSQL_ROOT_PASSWORD (contraseña root de MariaDB)
# - MYSQL_DATABASE (nombre de la base de datos)
# - JWT_SECRET (secreto para tokens JWT)
# - MASTER_PASSWORD (contraseña maestra para recuperación)
