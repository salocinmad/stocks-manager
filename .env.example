# ===========================================
# Stocks Manager - Configuración de Entorno
# ===========================================
# Copia este archivo a .env y rellena los valores

# Base de Datos (PostgreSQL)
DB_USER=tu_usuario_db
DB_PASSWORD=tu_contraseña_db

# Autenticación
# ⚠️ IMPORTANTE: Esta clave se usa también para ENCRIPTAR datos sensibles en la base de datos (API Keys, contraseñas SMTP).
# Si pierdes o cambias esta clave, NO podrás recuperar esos datos ni restaurarlos desde un backup.
# GUARDA UNA COPIA SEGURA DE ESTE ARCHIVO .ENV O DEL JWT_SECRET.
JWT_SECRET=cambia_esto_por_un_string_aleatorio_seguro

# API Keys (opcional - se pueden configurar desde el panel de admin)
FINNHUB_API_KEY=
GOOGLE_GENAI_API_KEY=

# URLs de la Aplicación
APP_URL=https://tu-dominio.com
FRONTEND_URL=http://localhost:3000
